# Harness Pocket（口袋 Harness）· 完整使用手册

> 目标：手机随时随地远程控制电脑上的 DeepSeek Harness。
> 接入优先级：**FRP 内网穿透（国内优化首选）→ Cloudflare 隧道（次选）→ Tailscale 私有组网（备用）**。

---

## 一、这是什么 / 整体原理

```
手机浏览器 ──HTTPS──> FRP/Cloudflare/Tailscale 入口 ──> 电脑上的远程网关 ──> DeepSeek Harness
（wss 加密）        （国内优化/次选/备用）          （令牌认证）          （127.0.0.1:3080）
```

- **Harness 本体**只监听 `127.0.0.1`（本机回环），公网、局域网都连不上。
- **远程网关** `gateway.js`：令牌登录 + 反向代理 + 手机界面适配。
- **FRP（国内优化首选）**：公司电脑运行 `start-frp.bat`，走国内服务器/内网穿透线路。
- **Cloudflare（次选）**：运行 `start-remote.bat`，生成临时公网地址。
- **Tailscale（备用）**：电脑和手机之间建立加密私有通道，免费提供 HTTPS 证书。

---

## 二、电脑端：选择接入方式

### 前置条件
- 电脑已装 **Node.js**（LTS 版，`https://nodejs.org` 下载）。
- 电脑上 **DeepSeek Harness 本身能正常运行**（即 `dsh web` 能打开 `http://127.0.0.1:3080`）。

### 方式 A：FRP 内网穿透（国内优化首选，用户自助注册）

1. 浏览器打开注册页面：

```text
https://register.zhkjdream.com
```

2. 输入用户名（小写字母/数字/中划线）和邀请码，点击注册。

3. 注册成功后会得到三样信息：

```text
访问域名：https://harness-zhangsan.zhkjdream.com
frpc.toml 配置
网关访问令牌
```

4. 把 `frpc.toml` 保存到公司电脑的本项目目录：

```text
F:\workspacecraftsmen\craftsmen\harnessapp\frpc.toml
```

5. 双击 **`start-frp.bat`**：
   - 首次运行会自动下载 `frpc.exe`
   - 自动检查并启动网关
   - 自动启动 FRP 客户端

6. 手机浏览器打开第 3 步分配的专属域名，输入网关访问令牌即可。

7. **建议再双击 `安装自愈守护.bat`**（只需一次）：注册一个每 5 分钟自检的计划任务——网关掉线自动拉起、FRP 隧道注册丢失（表现为 frp 的 404 页）自动重启 `frpc.exe`。日志见 `watchdog.log`，卸载：`schtasks /delete /tn HarnessRemoteWatchdog /f`。

### 方式 B：Cloudflare 临时隧道（次选）

1. 双击 **`start-remote.bat`**。
2. 等待生成 `https://xxxx.trycloudflare.com` 地址。
3. 手机浏览器打开该地址。

### 方式 C：Tailscale 一键安装（备用）

1. 双击运行 **`一键安装.bat`**。
2. 首次会弹出 Tailscale 登录链接，在浏览器里登录你的 Tailscale 账号。
3. 完成后记下脚本打印的 **手机访问地址** 和 **访问令牌**。

> 也可以随时用命令行 `node info.mjs` 再次查看地址和令牌。

---

## 三、手机端

1. 根据电脑端选择的接入方式：
   - FRP/Cloudflare：手机**无需安装任何客户端**，浏览器直接打开对应地址。
   - Tailscale：手机安装 Tailscale，登录**和电脑同一个**账号。
2. 手机浏览器（建议 Chrome / Edge / Safari）打开对应 HTTPS 地址。
3. 输入**访问令牌** → 即可进入 Harness 完整界面。

> 说明：Harness 界面用了较新的网页技术，请用 **Chrome / Edge / Safari** 等现代浏览器；部分老内核浏览器（如 UC、夸克）可能无法完整显示。

---

## 四、日常使用（以后每次）

1. 电脑开机（网关会**自动启动**，无需手动）。
2. 确认电脑上的 **Harness 在运行**（`dsh web`）。
3. 按所选方案启动对应客户端：
   - FRP：运行 `start-frp.bat`
   - Cloudflare：运行 `start-remote.bat`
   - Tailscale：确保 Tailscale 已连接
4. 手机打开对应 HTTPS 地址 → 输令牌 → 远程控制。

> 手机打不开时，最快的自救顺序：① 双击 `restart-frpc.bat`（重启隧道，顺带检查网关）；② 仍不行就看 `watchdog.log` / `gateway.log`；③ 装了自愈守护的话，等 5 分钟它多半已经自己修好了。

> 手机浏览器可「添加到主屏幕」，之后像 App 一样一键打开。

### 常用操作提示
- **切换工作区/会话**：点左上角侧边栏，选择工作区和会话（和电脑端是同一份数据）。
- **发消息**：底部输入框输入 → 发送。
- **看文件/轨迹**：手机端为控制主界面优化，复杂面板（轨迹/详情）建议在电脑端查看。

---

## 五、管理命令（高级）

| 操作 | 命令 / 方式 |
| --- | --- |
| 查看地址和令牌 | 双击运行 `一键安装.bat`，或命令行 `node info.mjs` |
| 手动启动网关 | `start-gateway.bat` |
| 停止网关 | 任务管理器结束 `node.exe`（gateway.js），或 `netstat -ano \| findstr 8443` 找 PID 后 `taskkill /PID xxx /F` |
| 关闭 HTTPS | `"C:\Program Files\Tailscale\tailscale.exe" serve --https=443 off` |
| 重新开启 HTTPS | `"C:\Program Files\Tailscale\tailscale.exe" serve --bg 127.0.0.1:8443` |
| 改访问令牌 | 编辑 `gateway.config.json` 里的 `token`，重启网关 |
| 重启 FRP 隧道（手机打不开时） | 双击 `restart-frpc.bat` |
| 安装自愈守护（每 5 分钟自检） | 双击 `安装自愈守护.bat`（或 `schtasks /create /tn HarnessRemoteWatchdog /tr "wscript.exe \"<目录>\start-watchdog-hidden.vbs\"" /sc minute /mo 5 /f`） |
| 查看自愈记录 | `watchdog.log`（单次手动检查：`node watchdog.mjs`） |
| 取消自愈守护 | `schtasks /delete /tn HarnessRemoteWatchdog /f` |
| 取消开机自启 | `schtasks /delete /tn HarnessRemoteGateway /f` |

---

## 六、配置文件说明

### `gateway.config.json`
| 字段 | 说明 | 默认 |
| --- | --- | --- |
| `port` | 网关端口 | `8443` |
| `host` | 绑定地址（`127.0.0.1` = 仅本机，最安全，不要改） | `127.0.0.1` |
| `target` | 转发目标（Harness 地址） | `http://127.0.0.1:3080` |
| `token` | 访问令牌（建议改成你自己的强随机串） | 已生成 |

可选字段（一般不用改）：

| 字段 | 说明 | 默认 |
| --- | --- | --- |
| `dshAuth` | 是否自动签发并注入 Harness 自己的会话 Cookie | `true` |
| `dshCredentialsPath` | Harness 凭据文件路径（内含会话签名密钥） | `%DSH_HOME%\.credentials.yaml` |

**改令牌**：编辑 `token` 字段，保存后重启网关。生成随机令牌：
```
node -e "console.log(require('crypto').randomBytes(16).toString('base64url'))"
```

---

## 七、常见问题（FAQ）

- **手机打不开 HTTPS 地址**：先确认使用的是哪种接入方式：FRP 用 `frpc.toml` 里的 `customDomains`；Cloudflare 用 `https://xxxx.trycloudflare.com`；Tailscale 才需要手机登录同一账号并打开 `https://电脑名.xxx.ts.net/`。不要打开 `http://127.0.0.1:8443`（那是电脑本机地址）。
- **电脑本机打不开网关**：请使用 `http://127.0.0.1:8443`，不要用 `http://localhost:8443`。部分机器上 8443 端口可能被 Docker/WSL 等程序占用，`localhost` 会解析到 `::1` 或别的服务导致连不上。
- **手机流量下打不开 Tailscale**：Tailscale 是私有加密网络，`100.x` 地址只有登录同一 Tailscale 账号的设备才能访问；联通/移动流量下到 Tailscale 中继可能很慢。国内优化首选改用 `start-frp.bat`（FRP），次选 `start-remote.bat`（Cloudflare）。
- **手机流量下 Tailscale 显示已连接但打不开网址**：先确认电脑端 `tailscale status` 能看到手机 `Online`。如果能看到但 `tailscale ping 手机IP` 超时，说明数据面不通。请在手机上：① 重新开关一次 Tailscale；② 保持 Tailscale 前台/屏幕亮着测试；③ 关闭系统省电限制，允许 Tailscale 后台活动、自启动；④ 开启系统 VPN 的“始终开启 VPN”；⑤ 把 Android“私人 DNS”设为“自动”或关闭。
- **手机提示 `ERR_NAME_NOT_RESOLVED`**：说明手机没有解析 Tailscale 的 MagicDNS 域名。优先检查 Android“私人 DNS”和浏览器“安全 DNS/Private DNS”，都设为“自动/关闭”，并在 Tailscale App 里打开“Use Tailscale DNS / MagicDNS”。临时可先用 `http://100.111.39.94:8443`（电脑 Tailscale IP + 网关端口）访问，它不需要 DNS 解析，且只在 Tailscale 内网可达。
- **提示 `'xxx' 不是内部或外部命令`**：这是 `.bat` 文件编码/换行被 Windows cmd 读坏导致的乱码报错，通常不影响后面的 `node gateway.js` 启动；请使用已修复的 GBK + CRLF 版本。如果仍出现，请重新从项目目录复制 `.bat` 文件，不要在编辑器里另存为 UTF-8/LF。
- **提示令牌错误**：看 `gateway.config.json` 的 `token`，或运行 `node info.mjs` 看最新令牌。
- **手机点文件预览不出来，提示「文件资源服务不可用」**：这是 Harness **客户端**自己的插件状态掉了（页面里负责读文件的「文件资源」服务没注册上），跟隧道、令牌、权限都无关——同一台电脑上换个全新浏览器走同一条线路点同一个文件是好的。
  - 立即恢复：手机上**刷新页面**（下拉刷新，或从主屏图标关掉再打开）。
  - 自动恢复：网关注入的小脚本会盯着预览区，出现这句提示就自动刷新一次（60 秒内不重复、最多自动刷 2 次，之后变成一个「点这里重新加载」的小按钮；输入框里有没发出去的内容时不刷）。
  - 多发生在手机页面长时间挂后台、或网络切换导致长连接反复重连之后；刷新一次即可恢复，不影响电脑端。
  - 自测：`node scratch/cdp-preview-flow.mjs`（无头浏览器按手机宽度走网关点文件）、`node scratch/selfcheck-mobile-heal.mjs`（自愈脚本行为）。
- **手机打开域名只看到一页英文 `The page you requested was not found ... The server is powered by frp. Faithfully yours, frp.`**：这**不是**网关或令牌的问题，而是 **FRP 服务端上这个域名当前没有已注册的代理**——请求到了 frps，但没找到对应隧道。常见原因是电脑上的 `frpc.exe` 虽然还在运行（任务管理器里能看到），但它在服务端的会话已经掉了（长时间挂着的“僵尸”连接、服务端重启过等），域名映射就没了。
  - 一键修复：双击 **`restart-frpc.bat`**（结束旧 frpc → 检查网关 → 重新建立隧道）。
  - 自己确认：`frpc.log` 里正常应能看到 `login to server success` 和 `start proxy success`；`node scratch/selfcheck-public.mjs` 可以走公网完整自检（登录页 → GUI → WebSocket）。
  - 区分两种“打不开”：**frp 的 404 页** = 隧道没注册（重启 frpc）；**502 / 无法连接** = 隧道在，但本机网关（8443）没跑（跑 `start-frp.bat` 或 `start-gateway.bat`）。
  - 想以后不再手动处理：双击 `安装自愈守护.bat`，每 5 分钟自动检查并修复这两类问题。
- **输完令牌登录成功，页面却只有一行英文 `dsh web authentication required; reopen the URL printed by dsh web.`**：这是 Harness 自己的第二层认证没过，不是网关令牌的问题。Harness 只认「用启动时打印的 `http://127.0.0.1:3080/?token=...` 换来的、按 Host 绑定的签名 Cookie」，而这个地址是电脑本机回环地址，手机永远打不开。网关现在会自动用本机密钥（`%DSH_HOME%\.credentials.yaml`）签一个注入进去，不需要人工操作。若仍出现：
  1. `gateway.log` 搜 `DSH-AUTH`：正常是 `已签发 harness 会话 Cookie`；若是 `未读到会话密钥`，说明凭据文件路径不对（`DSH_HOME` 默认 `C:\Users\<你>\.dsh`），可在 `gateway.config.json` 里加 `"dshCredentialsPath": "完整路径\\.credentials.yaml"`。
  2. 电脑上跑 `node scratch/selfcheck-gateway.mjs` 自检：期望 `GET /` 返回 200 HTML，WebSocket 打开 `$events` 流能收到 `ready`。
  3. 改过 `gateway.js` 必须重启网关才生效。
- **页面能开但一直转圈、日志里 WS 反复连接**：网关的 WebSocket 桥接已处理两个已知坑——客户端握手后立刻发出的第一帧会先缓存、等上游连上再补发；二进制帧一律转成 text 帧（Harness 流式通道只接受 text 帧）。若仍反复重连，把 `gateway.log` 里对应的 `WS CLOSE(...)` 行发出来定位。
- **进得去但界面空白/卡加载**：换 Chrome / Edge / Safari（老内核浏览器不兼容）。
- **工作区和电脑端不一样**：正常现象，手机浏览器是新环境，点侧边栏切到你要的工作区即可（和电脑端是同一份数据）。
- **改令牌后手机要重新登录**：手机清一下该站点缓存，或换隐私窗口重新输新令牌。
- **换了新电脑 / 重装系统**：把整个 `harnessapp` 文件夹拷过去，装好 Node.js 和 Harness，再双击 `一键安装.bat` 重新跑一遍。
- **Tailscale 换账号**：命令行 `"C:\Program Files\Tailscale\tailscale.exe" logout` 后重跑 `一键安装.bat`。

---

## 八、安全说明

- **零局域网暴露**：网关只监听 `127.0.0.1`，局域网内其它设备也无法直连。
- **令牌认证**：所有公网入口（FRP/Cloudflare/Tailscale Funnel）都依赖网关访问令牌。
- **建议**：
  1. 把 `token` 改成强随机串。
  2. FRP 的 `auth.token` 与网关令牌分开保管。
  3. 不使用时关闭公网入口（FRP/Cloudflare/Tailscale Funnel）。

> ⚠️ Harness 是「远程执行代码」工具，请务必保管好访问令牌，不要分享给不信任的人。

---

## 九、文件清单

```
harnessapp/
├─ start-frp.bat           # ★ 国内优化首选：FRP 内网穿透
├─ restart-frpc.bat        # ★ 手机打不开时：重启隧道 + 检查网关
├─ 安装自愈守护.bat        # ★ 注册每 5 分钟自检（网关 / 隧道自动恢复）
├─ watchdog.mjs            # 自愈守护逻辑（日志 watchdog.log）
├─ start-watchdog-hidden.vbs # 守护的隐藏启动器（供计划任务调用）
├─ frpc.toml.example       # ★ FRP 客户端配置模板
├─ start-remote.bat        # 次选：Cloudflare 临时隧道
├─ 一键安装.bat            # 备用：一键安装 Tailscale
├─ tailscale-serve.bat     # 备用：Tailscale Serve 设置
├─ 使用手册.md             # ★ 本手册
├─ gateway.js              # 远程网关（令牌认证 + 反向代理 + 手机适配）
├─ gateway.config.json     # 网关配置（端口/令牌/转发目标）
├─ info.mjs                # 查看地址和令牌
├─ start-gateway.bat       # 手动启动网关
├─ start-gateway-hidden.vbs# 隐藏启动网关（开机自启用）
├─ mobile.css / mobile.js  # 手机界面适配（注入到 Harness）
├─ scratch/                # 排查用自检脚本（selfcheck-public.mjs / selfcheck-gateway.mjs）
└─ tailscale-setup.exe     # Tailscale 安装包（首次自动下载）
```
